<?php

//1- function to choose the appropriate workout : 

function select_workout($num_days,$intensity,$goal){
    //it chooses the appropriate  workout based on the user's weight

    // the image name has this form : "numberDays_intensity_goal.png"
    //we will have the name formed based on input parameters
    // $path=(str)$numDays . "_" . (str)$intensity . "_" . (str)$goal . ".png";

    // return $path;
}